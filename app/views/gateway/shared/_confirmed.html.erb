<div>
  <em> Меняем : <strong><%= confirmed.currency_source.name %></strong></em>
  <em> на : <strong><%= confirmed.currency_receiver.name %></strong>
    (резерв = <%= confirmed.payment_system_receiver.reserve %>)
  </em>
</div>
  
<div>
  <em> Курс обмена </em>
  <strong>1.0 <%= confirmed.currency_source.name %> </strong> =
  <strong><%= confirmed.path_way.rate %> <%= confirmed.currency_receiver.name %></strong>
  <div>
    <div><em>Сумма обмена:</em><%= confirmed.summa %></div>
    <div><em>Сумма к получению:</em><%= confirmed.receivable_receive %></div>
  </div>
</div>
