require 'spec_helper'

describe Gateway::PaypalController do

  #Delete this example and add some real ones
  it "should use Gateway::PaypalController" do
    controller.should be_an_instance_of(Gateway::PaypalController)
  end

end
